using Microsoft.AspNetCore.SignalR;

namespace ProdeMundial.Web.Hubs
{  
        // Esta clase es el "servidor" de mensajes
        public class TournamentHub : Hub
        {
            // No hace falta poner métodos aquí por ahora, 
            // solo sirve como canal para enviar mensajes desde el servidor.
        }
    
}
